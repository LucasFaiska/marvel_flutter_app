import 'package:flutter/widgets.dart';

class AppearancesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Aparições'),
      ),
    );
  }
}